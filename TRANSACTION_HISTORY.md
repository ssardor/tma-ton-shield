# 📜 Transaction History Feature

## ✅ Что добавлено

TON Shield теперь поддерживает **полную историю транзакций** для подключенных кошельков!

### 🎯 Возможности

1. **Загрузка всех транзакций** через TON API (tonapi.io)
2. **Детальная информация** о каждой транзакции:
   - TON переводы
   - Jetton (токен) переводы  
   - Выполнение смарт-контрактов
   - NFT трансферы
3. **Пагинация** - "Load More" для загрузки старых транзакций
4. **Детекция скама** - автоматическое выделение подозрительных адресов
5. **Красивое отображение**:
   - Дата и время
   - Тип операции с иконками
   - Суммы и комиссии
   - Комментарии к переводам

## 🔧 Технические детали

### API Endpoint
```typescript
// Метод в apiClient
async getTransactionHistory(
  address: string,      // Адрес кошелька
  limit: number = 20,   // Количество событий
  beforeLt?: number     // Pagination cursor
): Promise<TransactionHistoryResponse>
```

### Пример использования
```typescript
import { apiClient } from '@/lib/api/client';

// Загрузить первые 20 транзакций
const history = await apiClient.getTransactionHistory(walletAddress);

// Загрузить следующие 20
const more = await apiClient.getTransactionHistory(
  walletAddress, 
  20, 
  history.next_from
);
```

### Типы данных
```typescript
interface TransactionHistoryResponse {
  events: TransactionEvent[];
  next_from?: number;        // Курсор для пагинации
  total_events?: number;
}

interface TransactionEvent {
  event_id: string;
  timestamp: number;         // Unix timestamp
  actions: TransactionAction[];
  is_scam: boolean;
  lt: number;                // Logical time
  fee?: number;              // В наноТОН
}

interface TransactionAction {
  type: 'TonTransfer' | 'JettonTransfer' | 'SmartContractExec' | 'NftItemTransfer';
  status: 'ok' | 'failed';
  simple_preview: {
    name: string;
    description: string;
  };
  
  // Детали конкретного типа транзакции
  TonTransfer?: { ... }
  JettonTransfer?: { ... }
  SmartContractExec?: { ... }
  NftItemTransfer?: { ... }
}
```

## 📱 Где найти

1. **Главная страница** → кнопка "Transaction History" (синяя, во весь ряд)
2. **URL**: `/history`

## 🎨 UI Features

### Карточки транзакций
- ✅ Зеленый фон для TON переводов
- 🔵 Синий фон для Jetton переводов
- 🟣 Фиолетовый фон для смарт-контрактов
- 🔴 Красная рамка для SCAM транзакций

### Статистика
- Общее количество событий
- Количество безопасных транзакций

### Бесконечная прокрутка
- Кнопка "Load More" для подгрузки старых транзакций
- Автоматическая пагинация через `next_from`

## 🔍 Пример API Response

```json
{
  "events": [
    {
      "event_id": "abc123...",
      "timestamp": 1733684400,
      "actions": [
        {
          "type": "TonTransfer",
          "status": "ok",
          "TonTransfer": {
            "sender": {
              "address": "0:...",
              "is_scam": false,
              "is_wallet": true
            },
            "recipient": {
              "address": "0:...",
              "is_scam": false,
              "is_wallet": true
            },
            "amount": 1000000000,
            "comment": "Payment for service"
          }
        }
      ],
      "is_scam": false,
      "fee": -3000000
    }
  ],
  "next_from": 64411573000001
}
```

## 📊 Поддерживаемые типы транзакций

### 1. TON Transfer
- Отправитель и получатель
- Сумма в TON
- Комментарий (опционально)

### 2. Jetton Transfer  
- Токен (название, символ, иконка)
- Количество токенов
- Отправитель и получатель

### 3. Smart Contract Execution
- Адрес контракта
- Executor (кто вызвал)
- Gas fee
- Operation code

### 4. NFT Transfer
- NFT адрес
- Отправитель и получатель

## ⚡ Performance

- ✅ Используется официальный TON API (tonapi.io)
- ✅ Лимит 20 событий за запрос
- ✅ Кеширование в useState
- ✅ Автоматический refresh через кнопку

## 🛡️ Security Features

1. **Scam Detection**
   - Красная рамка для транзакций с is_scam: true
   - Предупреждение для scam-адресов

2. **Address Validation**
   - Проверка всех участников транзакции
   - Выделение подозрительных контрактов

## 📝 TODO (Future)

- [ ] Фильтры по типу транзакции
- [ ] Поиск по адресу/сумме
- [ ] Экспорт в CSV
- [ ] График активности
- [ ] Группировка по дате
- [ ] Deep link на tonviewer.com

## 🎯 Пример использования

```bash
# 1. Подключите кошелек на главной странице
# 2. Нажмите "Transaction History"
# 3. Просмотрите все транзакции
# 4. Нажмите "Load More" для старых транзакций
```

---

**Powered by TON API** 🚀
